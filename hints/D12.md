Target: `gluedModule_finite_projective` — the glued module is finite projective
over A. Stacks Tags 0BP2 / 0BP6 (vector-bundle case of Beauville–Laszlo).

Strategy (Stacks 0BP6): the glued module `Ker(glueDelta)` sits in the arithmetic
square for a rank-`n` free datum. It is:
- finitely generated: it is the fiber product of `Fin n → Rf` (finite free over
  `A_f`) and `Fin n → B`, and by `hB : IsCompletionLike` descent along
  `A → A_f × B ⇉ Â_f` a f.g. module. Use faithfully-flat / Milnor-square descent.
- projective: locally free — after inverting f it is `Fin n → Rf` (free), and
  after completing it is `Fin n → B` (free); a module that is free on the
  standard cover of the Milnor square is projective (Stacks 0BP2).
Assemble `Module.Finite` and `Module.Projective` from:
`Module.Finite.of_surjective`, `Module.Projective.of_split`,
`Module.projective_of_...` (local-global), plus the exactness nodes
D01/D04. If a full proof is hard, split into (a) `Module.Finite` and
(b) `Module.Projective` as two independent `have`s.
Relevant: `Module.Finite`, `Module.Projective`, `LinearMap.ker`,
`Submodule.fg_...`, `Module.Projective.of_basis` on each cover piece.
