Target: `isCompletionLike_adicCompletion` — Stacks Tag 0BNJ.

Your previous attempt was essentially COMPLETE: both `surjective_mod` and
`ker_mod` were reduced to a single private lemma

  ker (AdicCompletion.evalₐ (Ideal.span {f}) n).toRingHom
    = Ideal.span {(algebraMap A (AdicCompletion (Ideal.span {f}) A) f) ^ n}

Finish by proving exactly that kernel identity, then reuse the rest verbatim.
Proof of the kernel lemma:
- `AdicCompletion.evalₐ I n : AdicCompletion I A →ₐ[A] A ⧸ (I^n • ⊤)` is the n-th
  transition/evaluation; its kernel is `(I^n)`-adic, i.e. `AdicCompletion.ker_evalₐ`
  / `AdicCompletion.transitionMap` API. Use `I = Ideal.span {f}`, so
  `I^n = Ideal.span {f^n} = Ideal.span {f}^n` (`Ideal.span_singleton_pow`).
- The kernel of `eval n` on the completion is `I^n` mapped in, i.e.
  `(Ideal.span {f}).map (algebraMap A B) ^ n`. Convert between
  `(algebraMap f)^n` and `map (span {f^n})` with `Ideal.map_span`,
  `Set.image_singleton`, `map_pow`, `Ideal.span_singleton_pow`.
Relevant: `AdicCompletion.evalₐ`, `AdicCompletion.eval`, `AdicCompletion.ker_eval`,
`Ideal.span_singleton_pow`, `Ideal.map_span`, `RingHom.mem_ker`.
