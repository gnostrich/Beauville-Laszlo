Target: `isSMulRegular_baseChange` — `IsSMulRegular (M ⊗[A] B) f`.
Stacks Tag 0BNI (flatness/regularity step of Beauville–Laszlo).

`f • ·` on `M ⊗[A] B` equals `(f • ·)_M ⊗ id_B = TensorProduct.map (f•) id`.
You must show this is injective. Key facts available as hypotheses:
`hM : IsSMulRegular M f`, `hBf : IsSMulRegular B f`, `hB : IsCompletionLike A f B`,
`hA : IsSMulRegular A f`.
Approach (Stacks): from `hM`, `0 → M →(f) M → M/fM → 0` is exact. Tensoring with B
gives `Tor₁(M/fM, B) → M⊗B →(f⊗1) M⊗B`. Injectivity of `f⊗1` ⇔ the connecting map
is zero, which follows because on `M/fM` multiplication by `f` is 0 and B is
`f`-torsion-free (`hBf`) and completion-like. Use
`IsSMulRegular` ↔ `Function.Injective (f • ·)` (`isSMulRegular_iff` / defn),
`LinearMap.lTensor`/`rTensor`, `TensorProduct.map`, and
`IsSMulRegular` transfer along the tensor. Relevant Mathlib:
`IsSMulRegular`, `Module.Flat`, `LinearMap.rTensor_injective`,
`TensorProduct.map_id`, `smul_tmul`, `TensorProduct.lift`.
