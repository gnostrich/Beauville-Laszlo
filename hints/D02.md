Target: `sqSnd_surjective` — surjectivity of the second map of the arithmetic
(Beauville–Laszlo / Milnor) square, Stacks Tag 0BNI.

`sqSnd (u,v) = map(toTensor)(u) - (v/1)` as a map
`M_f × (M⊗B) → (M⊗B)_f = LocalizedModule (powers f) (M⊗B)`.

Strategy: every target `t : (M⊗B)_f` is `LocalizedModule.mk w (⟨f^n, _⟩)` for some
`w : M⊗B`, `n : ℕ` (`LocalizedModule.mk_surjective` / `induction using
LocalizedModule.induction_on`). It suffices to hit each `mk w s`. Take `v = 0`
and `u = LocalizedModule.mk (w' ) (f^n)` where `w' ∈ M` with `w'⊗1 = w`? Not
general — instead use the SPLITTING: for `s = f^n`, `mk w ⟨f^n⟩ = (1/f^n)·(w/1)`
and `w/1 = mkLinearMap w = - sqSnd(0, w)`. Then multiply by the unit `1/f^n`
(f is invertible in the localization: `LocalizedModule` over `powers f`) and use
that `map(toTensor)` covers the `M_f`-component. Concretely show the image of
`sqSnd` is a submodule containing every `mk w ⟨f^n⟩`: it contains `w/1`
(via `(0, -w)`) and is closed under the `A_f`-action, and `A_f` contains `1/f`.
Relevant: `LocalizedModule.mk_surjective`, `LocalizedModule.induction_on`,
`LocalizedModule.mk_smul_mk`, `IsLocalizedModule.mk'`, `LinearMap.range`,
`Submodule.mem_...`. The hypothesis `hB : IsCompletionLike A f B` guarantees
the B-side is consistent; the surjectivity itself is localization-formal.
