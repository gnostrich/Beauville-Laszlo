Target: `glued_unique` — uniqueness/universal property: any `(u,v)` compatible with
the glueing datum has `u.prod v` injective with range = the glued module.
Stacks Tag 0BP2 (the equivalence of categories / descent).

You are GIVEN `hA1`: for every `f`-regular `P`, the arithmetic square is short
exact (`sqFst` injective, `Function.Exact sqFst sqSnd`, `sqSnd` surjective) —
this packages nodes D01/D04/D02. Use it with `P := N`.
- Injectivity of `u.prod v`: `u = gluedFst∘iso`, `hu : IsLocalizedModule (powers f) u`
  so `u` is injective on the `f`-regular `N` (`hN`) via `IsLocalizedModule.injective`
  (same lemma as H0/D01). Hence `u.prod v` injective.
- Range = gluedModule: `⊆` from `hcompat` (`glueDelta ∘ (u.prod v) = 0` means the
  range lands in `Ker glueDelta = gluedModule`). `⊇` from exactness `hA1`:
  an element of the glued module is a compatible pair, and short-exactness of the
  square for `N` shows it is uniquely `u.prod v` of some `n : N`. Use
  `Function.Exact.…`, `LinearMap.range_eq_top/…`, `Set.range`, `Submodule.ext`.
Relevant: `IsLocalizedModule.injective`, `IsBaseChange`, `Function.Exact`,
`LinearMap.prod`, `LinearMap.ker`, `Set.range_comp`.
