Target: `gluedSnd_isBaseChange` — the B-side projection of the glued (fiber-product)
module realises `B`-base change. Stacks Tag 0BP2 (module glueing).

You reduced this to the private lemma `scalar_pullback_exists`:
given `ρ x = algebraMap B Bf y` (x : Rf localized `A_f`, y : B), there is `a : A`
with `algebraMap A Rf a = x` and `algebraMap A B a = y`. This is the arithmetic
square applied to `A` itself (D-nodes for `M = A`): the fiber product of
`A_f = Rf` and `B` over `Bf = Â_f` is exactly `A`. Prove it from:
- `IsLocalization.Away f Rf` : `x = mk' a' (f^k)` (`IsLocalization.mk'_surjective`).
- `IsLocalization.Away (algebraMap A B f) Bf` on the Bf side.
- `hB : IsCompletionLike A f B` giving `A/f^n ≅ B/f^nB`, so an element that is
  integral (denominator-free) on the B-side and lies in `A_f` must come from `A`.
Concretely: `ρ x = y/1` in Bf forces the denominator `f^k` of `x` to be a unit
times something in the image of A; use `IsCompletionLike.ker_mod` /
`surjective_mod` to pull `y` back mod each `f^n` and glue. Relevant:
`IsLocalization.Away`, `IsLocalization.mk'`, `IsLocalization.eq`,
`IsScalarTower`, `IsCompletionLike` fields.
